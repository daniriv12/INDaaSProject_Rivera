 Three collectors:

- NSDMiner : network dependency
- apt-rdepends : software package dependency
- lshw : hardware dependency

These contain the necessary readmes (when applicable) on how to install within. Installation might differ between different systems.